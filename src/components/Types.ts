export interface WorkoutDetail {
  icon: string;
  text: string;
}

export interface MacroItem {
  value: string;
  label: string;
}

export interface ProgressStat {
  value: string;
  label: string;
}